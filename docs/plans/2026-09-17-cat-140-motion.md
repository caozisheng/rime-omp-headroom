# Cat 140-Column Motion Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Merge the enhanced 140-column cat animation from `rime-omp-pet` while preserving `rime-omp-headroom`'s merged-widget architecture and existing narrow-panel behavior.

**Architecture:** Replace only the bundled cat asset. Expand the shared pack and renderer width envelopes from 24 to 140. Keep existing `.ts` imports, validation behavior, merged-widget placement, and cropping semantics; do not import the source repository's unrelated alignment API or formatting churn.

**Tech Stack:** TypeScript, Bun tests, JSON pet packs, OMP extension UI component.

---

### Task 1: Prove the wider pack contract

**Files:**
- Modify: `tests/pet-domain.test.mjs`

**Step 1: Write failing assertions**
- Assert `validatePetPack(catPack).ok === true`.
- Assert `catPack.width === 140`.
- Assert the `happy` animation moves across at least 100 columns by comparing the minimum non-space column across frames.
- Assert `renderPetFrame(cat idle frame, 140)` returns five exact rows and `renderPetFrame(..., 139)` returns `[]`.

**Step 2: Run test to verify failure**
Run: `bun test tests/pet-domain.test.mjs`
Expected: FAIL because the current bundled cat remains 14 columns.

### Task 2: Prove merged-widget preservation

**Files:**
- Modify: `tests/pet-runtime.test.mjs`

**Step 1: Write failing runtime assertion**
- Start the real extension runtime with cat selected.
- Render the merged widget at a width large enough for the Headroom minimum box plus the 140-column frame.
- Assert the full 140-character cat row is present without truncation.

**Step 2: Run test to verify failure**
Run: `bun test tests/pet-runtime.test.mjs`
Expected: FAIL because the current cat frame is only 14 columns.

### Task 3: Migrate the asset and width envelope

**Files:**
- Replace: `packs/cat.json` from `C:/Users/zisheng/Documents/cao/00_code/github/rime-omp-pet/packs/cat.json`
- Modify: `src/pet/renderer.ts`
- Modify: `src/pet/validate.ts`

**Step 1: Replace the cat pack exactly**
Copy the source JSON byte-for-byte.

**Step 2: Raise both width limits**
- `MAX_PET_FRAME_WIDTH`: 24 → 140.
- `PET_MAX_WIDTH`: 24 → 140.
- Preserve all other target-repository code.

**Step 3: Run focused tests**
Run: `bun test tests/pet-domain.test.mjs tests/pet-runtime.test.mjs`
Expected: PASS.

### Task 4: Update necessary documentation

**Files:**
- Modify: `README.md`
- Modify: `docs/pet-merge-design.md`

**Step 1: Update current behavior documentation**
- State that bundled animation stages may be up to 140 columns.
- State that full large-motion animation requires enough panel width and narrower panels crop at the host boundary.
- Update stale fixed-width/risk language in the merge design; do not rewrite historical architecture.

### Task 5: Verify and clean up

**Step 1: Run formatter/checker fixes only if reported**
Run: `bun run check`

**Step 2: Run type checking**
Run: `bun run typecheck`

**Step 3: Run all tests**
Run: `bun run test`

**Step 4: Run leak scan**
Run: `bun run scan`

**Step 5: Smoke-test real data flow**
Run a temporary Bun expression importing the shipped cat pack, validation, and renderer; verify width 140, validation success, and a five-row 140-column render. Remove no permanent test artifacts because the expression is inline.

**Step 6: Remove temporary planning files**
Delete root `task_plan.md`, `findings.md`, and `progress.md`; retain this implementation plan and necessary product documentation.
